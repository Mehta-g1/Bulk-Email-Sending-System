[build]
builder = "NIXPACKS"
buildCommand = "pip3 install -r Email/requirements.txt && python3 Email/manage.py collectstatic --noinput"

[deploy]
startCommand = "gunicorn Email.wsgi:application --chdir Email --bind 0.0.0.0:${PORT}"